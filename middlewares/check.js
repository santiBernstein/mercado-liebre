var check = require()
const mainController = require('../controllers/mainController');

module.exports = {

    function (req,res,next) {

        if (req.url == http://localhost3000/products/create?user=admin){

         return res.next()  }

        else { res.render('/', mainController.index); }
        
    }
}